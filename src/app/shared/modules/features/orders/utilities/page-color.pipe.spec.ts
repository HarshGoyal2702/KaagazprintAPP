import { PageColorPipe } from './page-color.pipe';

describe('PageColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PageColorPipe();
    expect(pipe).toBeTruthy();
  });
});
