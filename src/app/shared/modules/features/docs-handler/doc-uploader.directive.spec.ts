import { DocUploaderDirective } from './doc-uploader.directive';

describe('DocUploaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DocUploaderDirective();
    expect(directive).toBeTruthy();
  });
});
