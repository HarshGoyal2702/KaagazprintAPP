import { AddressAnchorDirective } from './address-anchor.directive';

describe('AddressAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new AddressAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
